# Phase 5 - OAuth2 & SSO (`phase5/oauth/`)

Modules 17-19: Google, GitHub, OpenID Connect account linking.

**Package:** `com.lockify.phase5.oauth`

**Env vars:** `GOOGLE_CLIENT_ID`, `GITHUB_CLIENT_ID` (optional — bina env ke OAuth disabled)

**APIs:** `/api/oauth2/linked-accounts`
