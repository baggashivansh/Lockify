# Phase 1 - Core Authentication (`phase1/coreauth/`)

Modules 1-6: Register, Login, JWT, Refresh, Spring Security, RBAC.

**Package:** `com.lockify.phase1.coreauth`

**Key files:** `AuthService`, `JwtService`, `SecurityConfig` (in shared)

**APIs:** `/api/auth/*`, `/api/user/*`, `/api/admin/*`
