# Phase 6 - Advanced Authorization (`phase6/authorization/`)

Modules 20-22: Permission-based access, resource ownership, ABAC.

**Package:** `com.lockify.phase6.authorization`

**Key:** `AbacPolicyEvaluator` — department/location/owner rules from DB

**APIs:** `/api/resources` (CRUD with ownership + ABAC)
