# Phase 2 - Account Security (`phase2/account/`)

Modules 7-10: Email verification, forgot/reset password, password policies, account locking.

**Package:** `com.lockify.phase2.account`

**APIs:** `/api/account/verify-email`, `/api/account/forgot-password`, `/api/account/reset-password`

**Config:** `lockify.phase2.*` in application.yml
